{
  "name": "yt-history",
  "version": "0.0.0",
  "description": "Fetches youtube history using your stored browser cookies",
  "type": "module",
  "bin": {
    "yt-history": "./bin/yt-history"
  },
  "scripts": {
    "start": "bun src/cli.js",
    "dev": "bun --watch src/cli.js",
    "build": "bun build src/cli.js --outdir lib --target node",
    "test": "bun test"
  },
  "dependencies": {
    "puppeteer": "^24.39.1"
  }
}
